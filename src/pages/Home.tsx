import React from 'react';
import Layout from '../ui/features/layout/layout-component';

const Home = () => {
  return (
    <div data-testid="home">
      <Layout />
    </div>
  );
};

export default Home;
